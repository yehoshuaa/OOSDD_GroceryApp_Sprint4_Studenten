<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Grocery.App.ViewModels"
             xmlns:m="clr-namespace:Grocery.Core.Models;assembly=Grocery.Core"
             x:Class="Grocery.App.Views.BestSellingProductsView"
             x:DataType="vm:BestSellingProductsViewModel"
             Title="BestSellingProductsView">

    <!-- TITEL -->
    <Shell.TitleView>
        <Grid>
            <Label Text="Best verkochte producten"
                   FontSize="Large"
                   HorizontalOptions="Center"
                   VerticalOptions="Center" />
        </Grid>
    </Shell.TitleView>

   
    <Border Stroke="#C49B33" StrokeThickness="4" Padding="10" Margin="10">
        <CollectionView ItemsSource="{Binding Products}"
                        Margin="20"
                        SelectionMode="None"
                        EmptyView="Nog geen gegevens beschikbaar."
                        HorizontalOptions="Fill"
                        VerticalOptions="Fill">

            <CollectionView.ItemsLayout>
                <LinearItemsLayout Orientation="Vertical" ItemSpacing="6" />
            </CollectionView.ItemsLayout>

            <!-- ===== HEADER ===== -->
            <CollectionView.Header>
                <Grid Padding="8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="0.5*"/>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Label Text="#" Grid.Column="0"
                           FontAttributes="Bold"
                           VerticalTextAlignment="Center" />

                    <Label Text="Product" Grid.Column="1"
                           FontAttributes="Bold"
                           VerticalTextAlignment="Center" />

                    <Label Text="Totaal verkocht" Grid.Column="2"
                           FontAttributes="Bold"
                           HorizontalTextAlignment="End"
                           VerticalTextAlignment="Center" />

                    <Label Text="Voorraad" Grid.Column="3"
                           FontAttributes="Bold"
                           HorizontalTextAlignment="End"
                           VerticalTextAlignment="Center" />
                </Grid>
            </CollectionView.Header>

            <!-- uc11: productView -->
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="m:BestSellingProducts">
                    <Grid Padding="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="0.5*"/>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Rangnummer -->
                        <Label Grid.Column="0"
                               Text="{Binding Ranking}"
                               VerticalTextAlignment="Center" />

                        <!-- Productnaam (komt uit Model-basis: Name) -->
                        <Label Grid.Column="1"
                               Text="{Binding Name}"
                               LineBreakMode="TailTruncation"
                               VerticalTextAlignment="Center" />

                        <!-- Aantal verkopen -->
                        <Label Grid.Column="2"
                               Text="{Binding NrOfSells}"
                               HorizontalTextAlignment="End"
                               VerticalTextAlignment="Center" />

                        <!-- Voorraad -->
                        <Label Grid.Column="3"
                               Text="{Binding Stock}"
                               HorizontalTextAlignment="End"
                               VerticalTextAlignment="Center" />
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Border>
</ContentPage>
